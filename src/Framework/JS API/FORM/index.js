export const on = function(collectionName, collectionConfiguration) {
    console.log(`Form events registered for ${collectionName} with events ${collectionConfiguration}`);
}

/**
 * 
 * @param {Object} controlConfigurations 
 *  
**  {
        parent: $wrapper.find('.my-control'),
        df: {
            label: 'Due Date',
            fieldname: 'due_date',
            fieldtype: 'Date'
        }
**  },
 */
export const make_control = function(controlConfigurations) {
    console.log(`Form events registered for ${collectionName} with events ${collectionConfiguration}`);
}